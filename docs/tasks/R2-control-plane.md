# R2 — Control plane

**Deliverable:** SQLite state, HTTP API, revisions, CLI against it.
**Proves:** state has one owner.
· [00 §8](../specs/00-overview.md#8-milestones)

R2 turns R1's local database into the control plane: the full schema, an HTTP API
over it, and configuration revisions. The fleet objects are created here as
*state* — nodes, models, deployments and routes become rows and endpoints. Nothing
in R2 talks to a node or serves inference; that is [R4](R4-agent.md) and
[R3](R3-gateway.md).

The load-bearing constraint is R2-34: the CLI and the API must call the same core
functions. Every endpoint added here is a chance to grow a second implementation
by accident.

## Schema

Each task is the migration plus the type, its state machine where it has one, and
the constraints that keep it honest. · [08 §1](../specs/08-data-model.md#1-schema)

- [x] **R2-01** `node` — inventory, offer, constraints, `reboot_policy`, states `pending → approved → ready → draining → departed` · [00 §3](../specs/00-overview.md#3-object-model)
- [x] **R2-02** `refusal` — a node's rejection of a desired-state document, against node and revision · [12 §1](../specs/12-node-guardrails.md#1-where-they-apply)
  - *done:* `0017_refusal.sql`, keyed `(node_name, deployment_id)` and carrying the revision it was computed against, so an operator can tell "refused, and the configuration has not moved since" from "refused, and this is stale". Written by the heartbeat and surfaced by `nodary node show` ([R4-15](R4-agent.md))
- [ ] **R2-03** `backend` and `derived_image` · [04 §9](../specs/04-backends.md#9-registering-a-backend)
- [x] **R2-04** `model` and `staging`, keyed `(model_id, node_name)`, states `absent → staging → verifying → staged | corrupt` · [05 §3](../specs/05-catalog.md#3-staging)
- [x] **R2-05** `deployment`, states `defined → staging → preparing → starting → ready → stopped | failed`
- [x] **R2-06** `route` and `route_member`
- [x] **R2-07** `limits` keyed `(subject_kind, subject_id)` · [06 §4](../specs/06-gateway.md#4-throttling)
- [x] **R2-08** `usage` and `usage_daily`
  - *done:* usage is a separate chain from audit and is never conflated with it — different volumes, different retention · [00 §3](../specs/00-overview.md#3-object-model)
- [x] **R2-09** `policy` and `join_token`
  - *note:* both halves landed early, in the slices that needed them: `policy` in [R1d](../plans/R1d-policy.md) for R1-25 – R1-28, and `join_token` in [R1c](../plans/R1c-identity.md) for `nodary token join`. [08 §1](../specs/08-data-model.md#1-schema) was corrected to the `policy` shape that shipped
- [x] **R2-10** No two deployments on a node may claim the same GPU index
  - *done:* rejected at the control plane with `409` · [11 §2](../specs/11-failure-modes.md#2-models-and-deployments)
  - *note:* a unique index over `(node_name, gpu_index)` on a `deployment_gpu` child table, so the two writers that will race for it — an HTTP handler and an agent reconcile, which cannot see each other's transaction — are refused by the database rather than by whichever of them remembered. It cannot be scoped to live deployments: SQLite prohibits subqueries in a partial index predicate, so a claim lasts as long as its deployment and freeing a GPU means deleting one · [R2a](../plans/R2a-fleet-schema.md)
  - *deps:* R2-01, R2-05

## Revisions

- [x] **R2-11** `revision`: monotonic sequence, full snapshot, author, justification, hash-chained to its predecessor · [08 §2](../specs/08-data-model.md#2-revisions-replace-version-control)
  - *done:* every configuration change writes one; the chain verifies the same way the audit chain does, and `nodary config verify` names the first altered revision
  - *note:* a snapshot holds **desired state only** — no heartbeats, no staging progress, no deployment health. An export is "canonical, for provisioning and DR", and replaying a node's last heartbeat onto a rebuilt control plane asserts something false; it also decides what a diff means, since revisions differing because a node checked in would make `config diff` unreadable · [R2b](../plans/R2b-revisions.md)
  - *note:* it is its own chain, not the audit chain. Every revision is accompanied by an audit record naming who wrote it: the revision carries the state, the audit record carries the act
  - *deps:* R1-06, R2-01
- [x] **R2-12** `nodary config show|diff|rollback|export|apply` · [10 §1](../specs/10-cli.md#1-verbs)
  - *done:* `export` emits canonical TOML that `apply -f` reads back; the database stays authoritative and the file is a convenience. Proved across *databases* rather than within one — a restore onto a fresh install re-exports byte-identically, which is what "for DR" actually claims
  - *note:* objects absent from a file are left in place and reported unless `--prune`. Reconcile-to-absent assumes the file was generated by the same system, which `export` produces and a hand-written fragment does not, and the failure mode is losing a fleet to a partial file
  - *note:* `apply` cannot create a node. A node joins by enrolling, and a file that could conjure one would let a paste into the wrong terminal add a machine to the fleet
  - *deps:* R2-11
- [x] **R2-13** A rollback is itself a new revision
  - *done:* history is append-only; nothing is ever rewritten. There is no branch for rollback in the applier — every apply records a revision, so this holds by construction rather than by remembering
  - *deps:* R2-12
  - *deps:* R2-12
- [x] **R2-14** Retention and pruning as a periodic task · [08 §3](../specs/08-data-model.md#3-retention)
  - *done:* pruning writes an audit record naming the range removed, and refuses to prune `audit` below the active profile's floor. A retention job that silently deletes evidence is indistinguishable from tampering
  - **`nodary prune`, not a goroutine in `nodary-server`.** The spec says "periodic task" and the obvious reading is a ticker inside the server — but a prune must write a record, and a record needs an actor, a justification and an intent to bind. A goroutine has none, so it would need a synthetic principal invented for it, which is the one thing [07 §3](../specs/07-identity-audit.md#3-the-audit-chain)'s seam exists to stop. A verb gets the preview, the intent hash, the confirmation and the record for free; `nodary-prune.timer` supplies the period, which is what schedules things on this appliance already
  - **pruning the chain would have made the product report its own retention job as tampering.** `VerifyDB` verified a database with `requireGenesis`, on the correct reasoning that *"a database holds the whole chain, so a partial one there means records were deleted"*. So a prune records where it cut (migration 0019) and verification checks the survivors against that instead — reusing `audit.Anchor`, already exercised by a rotated sink file and by `export --from-seq`, where an anchored fragment proves as much as a whole chain from the anchor onwards
  - the anchor is **on `installation`, not a table of its own**: a prune only ever removes a prefix, so the latest cut is the only thing verification can ask about. The history of prunes is in the chain itself
  - what it does not claim: an anchor is written by whoever can write the database, so it defends nothing about the range that is gone — nothing can, once the records are. That is what `audit verify --mirror` is for, and it is why the floor exists
  - **the floor is enforced against sequence, not timestamp.** The prefix stops at the first record young enough to keep, so one stale timestamp among younger records — `audit.KindClockWentBack` exists because that happens — cannot drag the records inside the window out with it
  - the **tail is always kept**, whatever the window says. The prune's own record is appended to the same transaction afterwards and needs a predecessor; an emptied table would restart it at genesis while the anchor said otherwise, and the prune would refuse to verify itself
  - a pass that removes nothing **still writes its record**. That it ran and found nothing is what makes a gap in the history mean something
  - gated on `PermPolicyApply` rather than a permission of its own: what may be pruned is decided by `audit_retention_days` and `usage_retention_days`, so the role that sets those already decides this, and [07 §1](../specs/07-identity-audit.md#1-roles-and-permissions)'s table is vocabulary rather than a place to add to
  - *deps:* R1-26, R2-08, R2-11

## HTTP layer

- [x] **R2-15** HTTP server, TLS from `/etc/nodary/server.toml`, routes under `/api/v1` · [09](../specs/09-api.md)
- [x] **R2-16** Authentication: session cookie or `Authorization: Bearer nodary_pt_…`
  - *done:* short-lived session cookies honour `session_ttl_minutes`; a bearer token wins over a cookie when both are sent, because that is a script with a stale browser cookie far more often than the reverse
  - *note:* an HTTP caller is **never** local root. [R1c](../plans/R1c-identity.md)'s argument for that principal is filesystem access to the database; a network caller has none, and treating an unauthenticated request as an administrator would be the same reasoning applied where it does not hold
  - *deps:* R1-21, R1-26
- [x] **R2-17** The uniform error envelope and the full status-code table · [09 §3](../specs/09-api.md#3-errors)
  - *done:* `code` is stable and machine-readable; a route outside the caller's allowlist returns `403`, not `404`. The status table and the CLI's exit-code table walk the same `errors.Is` chain, so a policy refusal cannot be exit 5 in one front end and 500 in the other
  - *note:* a `500` never returns its own message. The text can carry a query, a path or a driver's own words; it is logged against the request id the caller was given instead
- [x] **R2-18** `?dry_run=true` returns the rendered change and its `intent_hash` without applying · [09 §2](../specs/09-api.md#2-conventions)
  - *done:* attestation is available to API clients, not only the CLI
  - *deps:* R1-13
- [x] **R2-19** `X-Nodary-Intent` enforcement — refuse with `412` when the re-rendered change no longer matches
  - *deps:* R1-14, R2-18
- [x] **R2-20** `X-Nodary-Justify` and `X-Nodary-TOTP` on mutating requests · [09](../specs/09-api.md)
  - *deps:* R1-15, R1-16
- [x] **R2-21** Pagination: `limit` (default 50, max 500), `cursor`, `next_cursor` · [09 §2](../specs/09-api.md#2-conventions)
  - **keyed, not offset-based.** Every listing is already ordered by its own key in SQL, and a cursor that is the last key returned stays correct when rows are inserted or removed between pages — where an offset silently skips or repeats one, exactly where paging matters most
  - a limit above the maximum is an **error, not a silent clamp**, the rule `audit.Filter` already states: a caller that asked for 5000 and was handed 500 has been given a wrong answer quietly, and will page through the wrong number of them without learning why
  - `next_cursor` is absent on the last page rather than empty, so a client stops without one more request that returns nothing. The audit and revision listings fetch one row past the page, so "is there more" is answered by the same query rather than a second count that can disagree with it
  - users are keyed on **name and id together**: `?all=true` can return a deleted user and a live one sharing a name, and a cursor on the name alone would step over the second. Tokens keep their newest-first order and page the other way rather than being reordered to suit the paginator
- [x] **R2-22** `If-Match` on versioned objects, `409` on mismatch
  - *done:* two administrators cannot silently overwrite one another
  - **the configuration revision is the version**, not a counter per object. Models, deployments, routes, limits and the active profile are all read out of one `config.Snapshot` and every change to any of them records one revision (R2-11), so a second counter beside it could only disagree with it. The cost is that it is conservative — editing a route is refused when somebody else changed a *limit* — and that is the right way to be wrong: the lost update it prevents is silent, and the failure it causes instead says what to do
  - **checked inside the mutation's own transaction**, never before it. Read outside, two administrators could both see revision N, both pass, and both apply — the exact race the header closes. `internal/store` caps the writer pool at one connection, so a check inside the transaction is serialized by construction
  - reads stamp an `ETag`, so a client has something to send back, and both the quoted form and a bare number are accepted — a client echoing the header and one that pulled the number out of the JSON are both being reasonable
  - its own code, `revision_changed`, rather than the shared `conflict`: re-read and retry is right here and wrong for a name already taken
  - *found while wiring it:* the API's `policy apply` preview was missing the `denies` list the CLI's has (R4-32), so an administrator over HTTP was not shown which registered models a profile would deny before agreeing to it. Both front ends now render the same preview, which is what the tracker's first cross-cutting constraint is for
- [x] **R2-23** `Idempotency-Key`: a repeat within 24h returns the original response rather than acting twice
  - **the row is written before the handler runs, not after.** Recording the outcome afterwards makes this a cache of responses, and a cache does not stop the case that actually happens: a client whose request timed out retries while the first attempt is still working, and both mint a token. Inserting first turns the key into an exclusion and the primary key enforces it. Proved by injection — the concurrent-retry test mints twice without it
  - **scoped to the caller.** A key is a client's own string, so two clients can choose the same one; handing one caller's response to another is a disclosure, not merely a wrong answer. The injection for this returns somebody else's freshly minted token
  - a key reused for a **different** request is refused rather than replayed: that is a client bug, and answering it with the old response hides the bug behind a plausible success. A request that **fails** releases its key, so one malformed attempt does not burn the key the client will retry with
  - a row left in flight by a process that died is **not** reclaimed by the next attempt. Nobody knows whether that mutation committed, and guessing "it did not" is how a second token gets minted; the retry is told to look, and the chain is where the answer is
  - **the stored response is sealed under `secret.key`.** `POST /tokens` returns the plaintext token, and [10 §4](../specs/10-cli.md#4-output) says that is shown exactly once and never readable again — a replayable copy in a column would make that untrue of the database file. A test reads the column and fails if the token is in it
  - every POST is wrapped **by default**, with an exemption list that states a reason per entry, because a new endpoint added without a thought about replay is the failure this exists to prevent
  - the writes live in **`internal/replay`**, exempt from `TestNothingBypassesTheSeam` the way `internal/observed` is and under a rule its package comment carries: it writes one table and nothing else. A row records that an HTTP request arrived, not something an operator did, and an audit record per retry would bury a month of administration — 0006_fleet.sql's argument for usage rows
  - spent keys are pruned by the retention pass (R2-14), because each holds a sealed copy of a response and one of those responses is a credential
- [x] **R2-24** Request IDs threaded from request through audit and usage records · [06 §6](../specs/06-gateway.md#6-error-envelope)
  - *done:* a user's report of one bad request resolves to one row without guesswork

## Endpoints

Grouped as [09 §1](../specs/09-api.md#1-surface) groups them. Each depends on
R2-34 rather than reimplementing behavior.

- [x] **R2-43** Failed authentication is throttled, and password verification leaves the write transaction · NIST SP 800-171 3.1.8
  - *done:* five failures in fifteen minutes locks a key out for fifteen minutes, counted against **both** the username and the connecting address. The username alone lets an attacker walk a user list, five guesses each, forever; the address alone lets one attacker on a shared NAT lock out an office. Either one trips
  - **the address comes from `RemoteAddr` and never from `X-Forwarded-For`.** There is no configured proxy in front of the control plane, so a forwarded-for header is attacker-controlled input, and trusting it would let one client present a fresh address per request and never be throttled at all
  - **`VerifyPassword` takes a `Querier`, not an `audit.Mutation`.** PBKDF2 at 600,000 iterations measures ~68ms and `internal/store` caps the writer pool at one connection, so the hash running inside the login's write transaction let roughly fifteen unauthenticated attempts a second hold the write lock continuously — blocking audit records, agent status posts and usage rows behind somebody who had not authenticated. The rehash still commits in the same transaction as the login that earned it; only the arithmetic moved out, and `Rehash` carries the result across
  - the upgrade is guarded on the hash it verified against, so a password changed between the read and the write is never silently rolled back by rehashing the old plaintext over it
  - **a locked-out attempt is refused before the audit write**, which closes the unbounded-growth half: an attacker can no longer append to the chain at the speed of a network
  - *note:* username enumeration is fixed here rather than separately, as the review said it compounds. Every failure — unknown account, suspended account, no password set, wrong password — returns `ErrBadPassword` with an identical envelope, and the real reason travels separately into the audit record. The timing oracle goes with it: an unknown account still pays for a hash, because answering in microseconds where a real account takes 68ms is the same leak by another route
  - *note:* in-process, so a control-plane restart forgives a lockout. An attacker who can restart the control plane has root on the machine holding the audit chain and does not need to guess passwords; persisting it would put a write on every failed attempt, on the connection this change exists to stop blocking
  - *note:* raised as §4.2's first item and §4.3's last · [2026-09-11 review](../review-2026-09-11.md)

- [x] **R2-25** Auth — `POST /auth/login`, `POST /auth/logout`, `GET /auth/whoami`
  - **a session holds a user id, not a principal.** It held a whole `identity.Principal` made at sign-in, so a session was a snapshot of who somebody was then: suspending an account left every open session of it working for the rest of the TTL — 10080 minutes under `default`, a week of continued privileged access after the decision to remove it. The token path never had this, because `identity.Authenticate` re-reads the user row on every request; the cookie path now resolves the same way, and a suspended session is dropped rather than merely refused
  - the cost is one indexed read against the reader pool per request, which is what a suspension taking effect *now* costs
- [x] **R2-26** Nodes — list, show, `approve`, `drain`, `revoke`, `verify-egress`
  - *note:* list, show, `approve` and `drain` are built **over the API**. `nodary node approve` and `node drain` are now CLI verbs too, building the same `core.Change` rather than a second one — the install's own closing line tells an operator to run `nodary node approve <name>`, and until then that meant creating an administrator, minting a token and reaching for curl to move a machine already sitting in front of them. Found the first time a model was deployed: everything applied, the node stayed `pending`, and the agent correctly had nothing to do
  - a **console** approval records no `approved_by`, because 0006_fleet.sql pairs that column with `approved_at` in a CHECK and a local principal has an actor but no user row ([07 §1](../specs/07-identity-audit.md#1-users-and-roles)). Both are left NULL and the chain carries who and when; inventing a user id would put a name in the fleet table that resolves to nothing
  - *note:* `list` and `show` are CLI verbs too, reading through [`internal/fleet`](../../internal/fleet/fleet.go), which the HTTP handlers now call as well — they had carried their own SQL while `nodary node list` was a stub, so the only way to see a fleet *from the machine hosting it* was curl with an administrator's token. Both name what an operator has to act on rather than only listing: a pending node with the approve line to run, a stale one with the unit to check, a node offering no GPU (an agent that could not find `nvidia-smi` — measured), and a ready deployment in no route, which serves nobody because LiteLLM routes by name
  - *note:* list, show, `approve` and `drain` are built. `revoke` and `verify-egress` act on a node that has enrolled and hold a certificate to withdraw or a namespace to probe, so their cores are [R4](R4-agent.md)'s — [R4-06](R4-agent.md) and [R4-29](R4-agent.md). Serving them now would mean serving something that cannot work
  - *done:* `revoke` is served. It could not be added by wiring a route alone — the API carried **its own copy** of the rules about which columns move with a node's state, and that copy wrote `state` by itself for anything that was not an approval, so a revoke over HTTP would have failed 0006_fleet.sql's CHECK pairing `departed` with a `departed_at`. Both front ends now call [`fleet.Transition`](../../internal/fleet/transition.go), which is where one set of those rules lives
  - *done:* `verify-egress`, and **there was no stored result to return** — that is what the sentence above was hiding. [R4-29](R4-agent.md)'s probe ran after every deployment start, reached a verdict, and wrote it to the node's own journal, where it stopped. The one place an assessor looks knew nothing about the control the whole CUI boundary rests on. So the verdict now rides the heartbeat and is kept against the deployment (`0021_deployment_egress.sql`), and the endpoint reads it
  - **a read, not a probe.** The assertion runs inside a deployment's network namespace on the machine hosting it, which the control plane cannot reach and must not try to: an endpoint that ran it across the network would hold a request open for three timeouts per deployment and would answer *"the node is unreachable"* for exactly the case an assessor most needs an answer to. `GET` rather than `POST`, because nothing here acts
  - **an absent verdict is not a verdict.** A report carrying none leaves the stored one standing, because the probe runs on a start and while an answer is inconclusive — not every fifteen seconds — so most heartbeats carry no new answer and blanking on each would make a node that asserted isolation ten seconds ago indistinguishable from one never asked. Null stays null for a deployment that has never run: nothing has been probed, which is neither compliant nor a failure
  - the node-level verdict is [`fleet.EgressState`](../../internal/fleet/fleet.go), and it is in one place because getting the rounding backwards is quiet. One non-compliant deployment is the node's answer however many compliant ones surround it; the node is compliant only when every deployment on it is; anything partial is `inconclusive`. `last_seen` and `stale` travel with the verdicts, because a stale node's compliant answer is a statement about a machine nobody has heard from
  - *found while building R4-06:* `drain` moved a node's state and nothing routed differently, because the gateway's rendering never read node state. A draining or departed node's deployments now leave their routes on the next `gateway sync`, which is what made drain mean anything from outside the database
  - `nodary node show` carries it too — a column in the deployment table, and a line on stderr for a deployment that has a way off the box or could not be shown not to. [11 §3](../specs/11-failure-modes.md) makes a failing assertion a critical alert, and a word in a table is not one
- [ ] **R2-27** Backends — list, show, create, delete, `build`, build status
- [x] **R2-28** Models — list, register, show, `enable`, `disable`, `restart`, `stage`, `unstage`, delete
  - *note:* list and show are built, and registration and deletion are declarative — they go through `config apply`, which is one applier rather than a second set of writers. `enable`, `disable`, `restart`, `stage` and `unstage` all direct a node to do something and are [R4](R4-agent.md)'s — all five are now built there (R4-35, R4-36), as CLI verbs calling `config.Apply`/`config.Record` or (for `restart`/`restage`/`unstage`, edge-triggered rather than declarative) the same audited ceremony. What R2-28 itself still lacks is the HTTP handlers for `enable`/`disable`/`restart` specifically — `nodary model enable` and its siblings have no `POST /models/{id}/enable` to call yet, so this stays open at the API surface even though the capability underneath it is done
  - *done:* the HTTP surface for `enable`, `disable` and `restart` — the half that was still missing. The edit is [`config.SetDisabled`](../../internal/config/apply.go) and the restart is [`fleet.RestartTargets`](../../internal/fleet/transition.go)/`RequestRestart`, which the CLI verbs now call too, so neither front end can disagree about which deployments a verb touches. `?node=` narrows enable and disable and is **required** for restart, which is inherently one machine's act
  - they are **their own actions, not `config.apply`**, even though enable and disable go through the same applier: [07 §1](../specs/07-identity-audit.md#1-roles-and-permissions) names `model.enable` and `model.disable`, and recording a disable as `config.apply` would make the chain answer "who stopped this model" with the wrong verb
  - *found by R2-44's tests:* **a deployment could be disabled and not seen.** `fleet.Deployment` carried no `disabled`, so `nodary node show` reported the settled state as `stopped` — which is also what an operator's own `systemctl stop` produces and what a node that has not polled since the change still reports. The verb that turns a deployment off had no verb that showed it was off. `node show` now says `stopped (disabled)`, and names the card the deployment still holds: disabling stops the unit and does not release the GPU, so an operator who disabled a model to free one is looking at a card that is idle and still spoken for
  - a model with no deployment is a **404 naming it**, not a successful no-op that leaves an operator believing something stopped. A disabled replica is skipped by a restart rather than failing it, and the response names which — an operator who asked for a restart and got one fewer than they have replicas needs to know why
  - *done:* `nodary model register` is the declarative path with the arithmetic done for the operator: it digests placed weights (`--source local`), or reads a manifest and lets the node's own agent fetch them (`--source remote`, R4-33), pins the backend image this build was tested against, and applies a model, a deployment and a route through `applySnapshot`. This verb itself downloads nothing either way and says where the weights or the manifest must be when they are not there. Before it, that whole step was `scripts/stage-model.sh` generating TOML, which no install places on a customer machine; the script now only downloads and writes the manifest — and `nodary install` (R5-31) can run that script itself, as the invoking operator rather than root, so an interactive install needs neither a separate script nor a manual permission grant
  - *found, and it was live:* **two deployments on one node could publish the same loopback port**, and `model register` defaulted `--port` to 8001 — so registering a second model on a node, without thinking about ports, produced exactly that. It is not a loud failure: the second container never binds while the first keeps serving, so the gateway's route for the second model reaches the *first model's* server. A request for one model answered by another, metered against the wrong one. Two claims on one GPU is the analogous failure and `0006_fleet.sql` guards it with a unique index; the port had nothing. `config.Apply` now refuses it, and the default is a starting point rather than an answer — `register` takes the first free port on that node and says which. Found by the check itself: two test fixtures were building the broken state
  - the refusal is a **`config.ErrInvalid`**, which is new and covers the model-and-node reference check beside it too. Both were unrecognized errors, and `internal/api` renders one of those as a 500 with the message withheld — so an operator's typo in a document came back as "the server failed to handle this request"
- [ ] **R2-29** Deployments — list, show, `logs`
  - *note:* list and show are built. `logs` reads a container's output on a node, which needs the agent
- [x] **R2-30** Routes — list, show, `PUT /routes/{name}`
  - *done:* the PUT edits the configuration and applies it through the same applier a whole-file `config apply` uses, so a single-object write records the same revision and cannot diverge from the declarative path
  - *done:* `nodary route list|show|set` (R4-36) are CLI verbs over this same read/write, not a second implementation — `set --add`/`--remove` resolve client-side and replace-write the whole route, exactly what the PUT handler already does
- [x] **R2-31** Users, tokens and limits — including `POST /tokens/join`
  - *deps:* R1-23, R1-24
- [x] **R2-32** Usage — `GET /usage?from&to&user&model&group_by`
  - the reader is **[`internal/metering`](../../internal/metering/metering.go)**, which `nodary usage show` also calls, so the two front ends cannot report different totals for one question. A package of its own rather than a function in `internal/observed`, whose charter is writes — *"holds the database writes that are observations rather than decisions"* — and a read belongs beside that, not inside it
  - dates go through `audit.ParseBound`, so `from=2026-09-01` means the same thing here as on `audit list`
  - **paged only when ungrouped.** A grouped report is ordered busiest-first, so a cursor keyed on the subject would step over rows — and it is bounded anyway by how many distinct subjects exist, where the ungrouped listing has one row per request and no bound at all. Truncating a report at 50 would hide its tail while looking complete
  - *gap, recorded rather than papered over:* **every authenticated caller can read everyone's usage.** [07 §1](../specs/07-identity-audit.md#1-roles-and-permissions) grants `usage.read.self` and `state.read` to the same role — `viewer`, the lowest — so there is no line in the table separating "mine" from "everyone's" for the handler to enforce. Narrowing by role rank would put an access rule where the table cannot be read to find it, and inventing `usage.read.all` would be vocabulary §1 does not define, the reasoning that kept `PermRoute*` out of `route set`. A site where a user must not see a colleague's token spend needs that permission to exist first; `nodary usage show` has the same reach today. A test pins the behavior, so adding the permission is a change something fails on
  - *deps:* R2-08
- [x] **R2-33** Audit, policy and config — listing, `verify`, `export`, `apply`, `diff`, revisions, rollback
  - *deps:* R1-09, R1-11, R1-28, R2-12

## The shared core

- [x] **R2-34** One set of core functions behind both the CLI and the API · [10 §1](../specs/10-cli.md#1-verbs)
  - *done:* a behavioral difference between `nodary model enable …` and `POST /models/{id}/enable` is impossible by construction, because neither holds business logic. This is a constraint on the implementation, not an aspiration — and it is cheapest to satisfy now, while there are two callers rather than three
  - *done:* [`internal/core`](../../internal/core/core.go) holds the whole of [07 §2](../specs/07-identity-audit.md#2-attestation); both front ends build a principal, a ceremony and a change and call `core.Act`. What stays in a front end is only what the core cannot know — where a credential arrived, whether there is a human to prompt, and how to render an outcome
  - *done:* asserted behaviorally, not only structurally: [`parity_test.go`](../../internal/api/parity_test.go) drives both front ends against one database and requires the same refusals for the same reasons. That both call `core.Act` is visible in the source; whether they therefore *behave* the same is what the constraint is actually about
  - *deps:* R1-12

- [ ] **R2-44** `--server URL` — the operator CLI acting on a control plane over the network, as the person running it · [10 §2](../specs/10-cli.md#2-global-flags)
  - **this is not a convenience, it is the attribution.** Without it the only way to run a privileged verb is a shell on the control-plane host, so `resolvePrincipal` falls to `localPrincipal` and the chain answers "who approved this node" with the name of an account several people share and a method of `local`. Every other gap on the roadmap is something the product does not do; this one is something it records incorrectly
  - *done:* the credential half. `nodary login` reads a personal token on stdin, checks it against the appliance with `GET /auth/whoami`, and stores it in `~/.nodary/credentials` under a key normalized from the URL, beside the certificate fingerprint that appliance is pinned to. `nodary logout` forgets one entry
  - **a personal token rather than a password**, which is the identity model already here rather than a second one: `token create --save` writes the same file for the local target, and [10 §1](../specs/10-cli.md#1-verbs)'s wizard is explicit that the one-time setup link exists so a password is never typed at a terminal. `POST /auth/login` stays what R2-16 built it for — a browser, in R7
  - **never as a flag.** A credential on a command line is in the shell history and in every `ps` for as long as the command runs, and unlike a join token — single-use, spent within the minute — a personal token is good for ninety days
  - **the pin is required on a first login**, not taken on trust. Trust on first use would make pinning decorative for exactly the operator who has never connected before; the fingerprint is the one `server install` prints and `server status` reprints, and is the same value a node pins
  - the exit codes are a **second table beside `statusFor`**, which is the risk this carries: a refusal the control plane can state and the client has no meaning for would arrive as a generic failure, so the same refusal would cost a script one code locally and another remotely. `remote_test.go` reads `internal/api/errors.go` and holds the client to covering every code it can produce
  - *done:* the reads — `node list`, `node show`, `route list`, `route show`, `limits show`. Each is one branch replacing the line that opened the database, because the endpoint and the verb already call the same `internal/fleet` and `internal/config` functions (R2-34) and everything downstream of that line is the same rendering. `--server` and `--db` together are refused: resolving that quietly is the failure this flag is most careful about
  - the listings **follow the cursor to the end**. The CLI's listings are not paged — `node list` prints the fleet — so stopping at the first page would print fifty nodes of sixty and say nothing, which is the wrong answer without a symptom that [09 §2](../specs/09-api.md#2-conventions) refuses a clamped limit over
  - *found by the parity test:* an empty listing came back as `null` over the network and `[]` locally, because a Go slice nothing was appended to marshals as `null`. `--format json` is a stable schema a script reads, so that is a real difference between two routes to one answer, with no reason behind it
  - *done:* the ceremony, and with it `node approve`, `node drain` and `node revoke`. Two requests where the local route makes three calls into `core` with a database handle between them: `?dry_run=true` renders the preview and returns the hash, and the act sends it back as `X-Nodary-Intent` beside `X-Nodary-Justify`. **Nothing about ceremony is decided on this side** — the preview is rendered by the same `Render`, the hash is the same `core.Preview` hash, and `core.Act` on the far side is what enforces the justification floor and the TOTP requirement. A front end that decided any of it would be the second implementation of attestation the arrangement exists to prevent
  - the TOTP prompt still belongs to the front end, because the control plane has nobody to prompt. It refuses with [09 §3](../specs/09-api.md#3-errors)'s `reauthentication_required`, the CLI asks, and the act is retried — the same shape the local route has for `core.ErrTOTPRequired`
  - *asserted, not described:* a `node approve` run over `--server` writes a record whose actor is the account the credential belongs to and whose method is not `local`, and the same act on the host writes `root`/`local`. That test is the task
  - `--justify` travels in a header, so a line break in one is refused with a message rather than by `net/http` — the only place the transport shows through
  - *done:* `model enable`, `model disable` and `model restart`, through the endpoints that run the same `config.SetDisabled` and `fleet.RestartTargets` the local verbs run. `--node` narrows a toggle and is required for a restart, and it narrows it on the far side too — a flag accepted here and dropped on the wire would turn "disable it on gpu-02" into "disable it everywhere", which is an outage rather than a change, so that is what its test asserts
  - *done:* `route set` and `limits set`, the two `PUT` verbs. `limits set` builds its whole object from flags and reads nothing first, so it is a plain replace; `route set` reads the route, edits it and replaces it, which is **three steps where the local route has one transaction** — so the membership it writes is built from what it read, and between the read and the write somebody else can change the same route. `If-Match` closes that window: the revision the read saw travels with the act. Conservative, because the version is the whole configuration's, and being wrong that way is a `409` saying what to do where being wrong the other way is a member silently disappearing
  - the edit itself is one function both routes call, so the local snapshot edit and the `PUT` body cannot produce different membership for the same flags
  - **one refusal, two roads, one exit code** — and it was not automatic. [09 §3](../specs/09-api.md#3-errors)'s stable codes and [10 §5](../specs/10-cli.md#5-exit-codes)'s exit codes are two tables filled in at different times, and building the client found three places they had drifted: `config.ErrInvalid` shared the code `invalid` with `identity.ErrBadName` while the CLI answered them 1 and 2 (split into `invalid_configuration`, which is exit 1 — §5 reserves 2 for bad flags and missing arguments, and a document is neither); `model disable` on a model with no deployment was `ErrBadName` in the CLI and `ErrNotFound` in the endpoint, for the same condition; and three applier refusals that name an operator's mistake in a document were unwrapped, so they reached a client as a 500 with the message withheld — including the one whose own comment says it exists so *"the refusal names the file's mistake"*
  - `identity.ErrNotFound` read **"no such user"** while `internal/api` already wrapped it for nodes, models, deployments and routes, so a 404 for a node came back as `no such user: no node named "gpu-09"`. Its text is neutral now
  - *done:* `token list`, `token create` and `token revoke`, which closes the loop: an administrator on their own machine can now mint the credential the next administrator logs in with, without a shell on the control plane. `--save` files it under the appliance it authenticates against rather than under `local`, and keeps that appliance's pin
  - *found:* **`POST /tokens` did not enforce the profile's credential lifetime.** It minted ninety days whatever was asked for, so it ignored the caller *and* a profile capping credentials at less — `token_max_ttl_days` bound the CLI and not the API beside it. That is the control [R1-17](R1-core-audit-identity.md)'s sibling was added for, and the CLI's own comment records that it went unread until a review minted a ten-year service key under a profile capping them at one. The lifetime now travels as the operator wrote it, is parsed by the same parser, and is checked by the same `attest.AllowTokenLifetime`
  - `attest.ErrLifetimeTooLong` was in neither error table: it reached `ExitPolicy` only because `token create` returned that code at its call site, and `statusFor` had no case for it at all, so a profile refusal over HTTP was a 500
  - *done:* `audit list` and `usage show`. `audit list` asks for one page of exactly `--limit`, not the cursor followed to the end: both caps are 500, and following it would quietly hand the whole chain to somebody who asked for fifty records
  - *found, and it is the worst one:* **[`audit.Record`](../../internal/audit/record.go) had a marshaler and no unmarshaler.** `encoding/json` matches an untagged field by name, so `action`, `seq` and `hash` came back from `GET /api/v1/audit` and `intent_hash` and `prev_hash` — the two whose names have two words — came back empty. **`prev_hash` is the chain.** A copy of the records read over the API had no links between them and no binding between an approved preview and what was applied, while looking complete; and an empty `prev_hash` does not read as an incomplete export, it reads as tampering ([13](../specs/13-evidence.md) is built on exactly that distinction). The inverse is written now, and `record_test.go` holds the two to each other by round trip — reflectively requiring every field of the fixture to be non-zero first, so a member added to `members()` and forgotten fails without anybody extending a list
  - a second `null` where `[]` belongs, in `metering.Query` this time: a stable schema's empty listing is `[]` on both roads
  - *open:* `user show` and `user suspend` need endpoints before they can be wired, and `config` is the remaining read
  - the parity is a test rather than a review: the same refusal is driven down both roads and the exit codes compared. `remote_test.go` holds the second half — every code `statusFor` can produce is named in the client's table, so a refusal added on the server cannot arrive here as a generic failure
  - *done:* `user add`, `user list` and `user delete`. `user suspend` and `user show` have no endpoint to call, so they refuse rather than being approximated — deleting in answer to a suspension would meet a reversible request with an irreversible act
  - **the email address is withheld by role, not by omission.** `GET /users` is gated by `PermStateRead`, so every viewer in the fleet can read it, and [07 §1](../specs/07-identity-audit.md#1-users-and-roles) gives a viewer "read state" — a contact address is personal data attached to a person rather than fleet state, and a listing that hands every viewer every address is a harvest. It travels with `PermUserManage`; nothing else is withheld, because when an account was created is the same kind of fact as its role
  - *found:* **the identity endpoints answered narrower than the verbs they mirror**, which R2-31 marks done. `GET /tokens` returned id, user, kind, prefix, revoked and unattended where `nodary token list` renders the label, the expiry, when it was last used, a one-word state *and the outstanding join tokens* — so "is there a join token outstanding" and "what expires this week" were unanswerable over HTTP. `?user=` took the user's **id** while `POST /tokens` takes their **name**, so `?user=alice` matched nothing and said so by returning an empty list. `POST /users` and `POST /tokens` returned three fields of what they had just made
  - the fix is one projection rather than a wider map: [`identity.UserReport`](../../internal/identity/report.go), `TokenReport` and `JoinReport` are what both front ends render. They were `internal/cli`'s, which is why the drift was invisible — a listing is exactly where R2-34's constraint stops being checked, because nothing calls a renderer twice A verb that has not been converted **refuses** rather than running: Go's flag package rejects an undefined flag, so the refusal cannot be forgotten for a new verb and only its wording is written down here. An operator believing they acted on an appliance across the room while the command edited this host's database is the failure that matters

## Server lifecycle

- [x] **R2-35** `/etc/nodary/server.toml` — bind address, TLS certificate paths, data directory · [01 §12](../specs/01-install.md#12-filesystem-layout)
- [x] **R2-36** `nodary server install|start|stop|status` · [10 §1](../specs/10-cli.md#1-verbs)
  - *note:* R2 covers the lifecycle verbs against an already-provisioned host; the full interactive install with preflight and component resolution is [R5](R5-install.md)
- [x] **R2-37** `nodary backup create|restore` · [08 §4](../specs/08-data-model.md#4-secrets-at-rest)
  - *done:* `secret.key` is captured by default, the command refuses a world-readable destination, and the output states plainly that a database backup without the key is useless. The inverse is the real risk — an operator who backs up only the database discovers at restore time that every agent must re-enroll
  - **`VACUUM INTO`, not a file copy.** In WAL mode the database is three files that disagree between checkpoints, so `cp nodary.db elsewhere` on a running control plane silently omits every record since the last one — and it is discovered at restore. `store.Snapshot` reads the whole database in one transaction and writes a consistent single file while the control plane keeps serving
  - **the whole configuration directory, with no exclusion list.** A list of what matters is a list somebody has to remember to extend, and the cost of forgetting lands on somebody already having a bad day. `/etc/nodary` is small and all of it is configuration or a secret
  - **the agent CA is why "database plus key" is not enough.** Its certificate and sealed key live beside `secret.key` rather than in the database, so a backup of the two the specification names would still make every node re-enroll. Capturing the directory covers it
  - restore **starts nothing**, and refuses an existing database or sealing key without `--force`: a database from one moment beside a sealing key from another does not announce itself, it fails weeks later at a TOTP prompt or a node's next request
  - restore also refuses an archive holding a database and no `config/secret.key` — caught before anything is written rather than at that TOTP prompt. A member path that is not local is refused too; an archive is untrusted input even when we wrote it
  - modes are recorded and reapplied explicitly, because `O_CREATE` applies the process umask and a sealing key restored 0644 instead of 0400 is a silent total compromise
  - *note:* raised as the review's fourth blocker — "that sentence describes nothing that runs" · [2026-09-11 review](../review-2026-09-11.md)
  - *deps:* R1-04
- [x] **R2-38** `nodary status` and `nodary restart` · [10 §1](../specs/10-cli.md#1-verbs)
  - *done:* one verb for a host that is **both roles**, because a `--with-node` box is one machine. `server status` reads server.toml and the chain, `agent status` reads the node's side, and neither says whether anything is actually *up* — so the operator asking "is this appliance working" had to know which roles the host carries, run two verbs, and then reach for `systemctl status` for the part that was the whole question
  - deliberately **shallow and fast**: no chain verification, no network, no probes. It names which of its siblings to reach for — `nodary doctor` is the verb that goes looking — because a status command that takes twenty seconds on a large chain is one an operator stops running
  - it exits the way `systemctl is-active` does, which is the contract somebody scripting it already knows: zero when what is installed is running, nonzero when it is not. Two exceptions, and they are what makes that usable — a unit with no unit file was never installed and is not a stopped service (a control plane has no `nodary-agent.service`), and `nodary-prune.service` is a `Type=oneshot` a timer triggers, so it is inactive between runs by design
  - `systemctl show` rather than `is-active`, because `is-active` exits nonzero for *both* "installed and stopped" and "never installed", which are different problems with different answers. Read by property name rather than by line order: `show` emits properties in its own order, not the order they were asked for
  - **`restart` is not a synonym for one `systemctl restart`.** `nodary server stop` refuses to exist for exactly that reason. What this knows and the operator does not is the *set* — which units this host's roles own, and that the data plane goes before the API that proxies to it. **containerd is deliberately not in it**: it is a shared runtime nodary installs rather than owns, and bouncing it stops every container on the machine, every deployment included, to fix something that is almost never containerd
  - no audit record. It changes nothing a chain is about — no identity, no configuration, no fleet state — and the control plane is the process being restarted, so the record would have to be written by the thing going away. `systemctl` and the journal are where a service restart is recorded, and where an assessor already looks for one
- [x] **R2-39** Self-signed certificate generation with the fingerprint printed · [01 §5](../specs/01-install.md#self-signed-control-planes)
  - *done:* the printed form includes the `--pinnedpubkey` variant of the node install command
- [x] **R2-40** The internal CA that signs agent certificates, unrelated to the server's public TLS certificate · [01 §4](../specs/01-install.md#4-server-install)
  - *done:* the CA private key is encrypted at rest under `secret.key`
  - *deps:* R1-04
- [x] **R2-41** A network audit sink — Elastic, Splunk HEC, or a generic NDJSON endpoint · [07 §3](../specs/07-identity-audit.md#3-the-audit-chain)
  - *done:* an implementation of R1-08's `Sink` and nothing else changes; the endpoint comes from `server.toml` and a destination that fell behind is re-synced with `audit export --from-seq` rather than by replaying from a queue
  - **asynchronous, and that is the load-bearing decision.** `Delivery.Emit` runs on the mutation's own goroutine right after the commit, so a synchronous POST would put a network round trip — and, when the far end is wedged, a timeout — between an operator pressing enter and their command returning. The record is already in the database by then, and the database is the authoritative copy
  - **a full queue drops rather than blocks**, which is the same reasoning followed to its end: blocking would reintroduce exactly what going asynchronous removed. Proved by injection — making it block hangs the test outright. The drop is reported like any other delivery failure and names `audit export --from-seq`, which is why there is no durable spool here to go wrong in its own right
  - a sink reporting asynchronously can only tell its caller about the batch *before*, and `Delivery` already renders that correctly: it announces a transition rather than a per-record result, so the failing-map and the `block` posture work unchanged
  - **the credential is a file of its own, not a field in `server.toml`.** The task said "sealed under `secret.key`"; it is 0600 in `/etc/nodary/audit-sink.token` instead, for the reason `paths.ModeMasterKey` already gives — and because `server.toml` is read by anyone diagnosing the control plane and pasted into support threads. It holds the **whole Authorization header value** rather than a bare token, so Splunk's `Splunk …`, Elastic's `ApiKey …` and a plain `Bearer …` all work without this package learning any of their dialects. Read per batch, so rotating it needs no restart
  - **plaintext `http` is refused except to a loopback address.** Audit records say who did what, when, and to which object. The loopback exception is the ordinary deployment rather than a loophole — a shipper on 127.0.0.1 owning the TLS to the SIEM — and refusing it would push operators to turn TLS off somewhere worse. A credential in the URL is refused too, and the refusal redacts it
  - configuration is in `server.toml` because the systemd unit says "Edits are overwritten" and means it; **the environment still overrides the file**, which is the opposite of the usual order and deliberate — the file is what a site runs, and the variable is the escape hatch for one command against a copy of a database
  - **a broken `[audit]` block fails the command** rather than falling back to the local file: an appliance configured to ship its chain and quietly not doing so is [pilot §1](../plans/pilot.md#1-the-row-mvpmd-2-was-missing)'s failure exactly. Only that block — `LoadAudit` reads it alone, so a typo in a TLS path does not fail `nodary backup create`, which is the command somebody reaches for while putting a broken appliance back together
  - *found by the tests:* batches went out with the head at the tail. The line that woke the worker was appended *after* the drained ones, so a receiver ordering by arrival recorded the chain going backwards
  - *done:* **a syslog sink, for the site that has no SIEM at all** — which is most of this market. The default sink is the JSONL file, and that mirror is on the same disk as the chain it mirrors, so it proves nothing against somebody who owns the appliance; [07 §3](../specs/07-identity-audit.md#3-the-audit-chain)'s whole argument is a copy that has already left the machine. `syslog:` hands the destination problem to what the site already runs — rsyslog forwarding, an MSP agent, a NAS, a firewall — and nodary learns nothing about it. `log/syslog` is stdlib, so it is an implementation of R1-08's `Sink` and nothing else moves
  - the **`authpriv` facility**, not `local0` or `daemon`: every default rsyslog configuration already routes authpriv to a root-readable file, where `local0` needs the site to configure something first and a sink that lands in `/var/log/messages` beside cron chatter is one nobody reads
  - the **port is required**, not defaulted to 514. Collectors listen where the site put them, and guessing wrong fails by delivering nothing to a port nobody reads — which is the failure this sink exists to prevent. Dialed when it is configured rather than at the first audited act, for the same reason
  - **syslog truncates, and that is written down rather than implied.** An RFC 3164 receiver may cut a message at 1024 bytes, so a record with a large detail can arrive halved — which breaks verification *of that copy*. So the syslog stream is the monitoring and alerting copy and the file mirror plus `audit export --from-seq` stays the evidentiary one. The line is emitted as unmodified canonical JSON: a sequence prefix would survive truncation better and would stop every JSON-aware collector reading any of it
  - *note:* deferred out of [R1b](../plans/R1b-audit-chain.md) deliberately. Shipping records to a SIEM with WORM retention is what a CMMC deployment needs and no MVP install does, and its configuration has nowhere to live until R2-35. The seam, the per-record `install` id and the `v` field that lets the record shape grow all land in R1-08 so this stays additive
  - *deps:* R1-08, R2-35, R1-04
- [x] **R2-42** PBKDF2-SHA256 password hashing, and the rehash-on-verify that lets its cost be raised · [07 §1](../specs/07-identity-audit.md#1-users-and-roles)
  - *done:* the parameters and their version are stored with each hash, so raising the cost does not invalidate an existing one; a hash produced under older parameters is replaced on the next successful verification
  - *done:* the salt is at least 128 bits — Go's FIPS module refuses anything shorter, so this is a correctness requirement and not a preference. Go enforces no iteration floor, so the cost is ours to pick · [spike §2](../spike-fips-and-manifest.md#2-on-and-only-are-different-products-and-the-difference-is-the-finding)
  - *note:* PBKDF2 rather than argon2id, which is stronger and not FIPS-approved · [ADR 0006](../adr/0006-cui-boundary-and-fips.md). Deferred out of [R1c](../plans/R1c-identity.md). A password's only consumer is R2-25's login, R1 authenticates with personal tokens, and [01 §9](../specs/01-install.md) has the first one set through a setup URL that arrives in [R5](R5-install.md) — so choosing parameters in R1 would mean choosing them against no login path at all. Nothing hashes a `user` row, so the column is additive
  - *deps:* R1-18, R2-25
